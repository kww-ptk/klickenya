import listing from '../schemas/listing'
import blogPost from '../schemas/blogPost'
import property from '../schemas/property'
import destination from '../schemas/destination'
import neighbourhood from '../schemas/neighbourhood'
import page from '../schemas/page'
import siteSettings from '../schemas/siteSettings'
import author from '../schemas/author'
import agent from '../schemas/agent'
import homePage from '../schemas/homePage'
import {
  quickFactsBlock,
  tipCardBlock,
  dayCardBlock,
  photoRowBlock,
  statRowBlock,
  budgetTableBlock,
  packingListBlock,
  pullQuoteBlock,
  inlineListingBlock,
  compareTableBlock,
  verdictCardBlock,
  whoIsItForBlock,
  destinationSectionBlock,
  distanceChipsBlock,
  deciderGridBlock,
} from '../schemas/blocks'

export const schemaTypes = [
  listing,
  blogPost,
  property,
  destination,
  neighbourhood,
  page,
  siteSettings,
  author,
  agent,
  homePage,
  quickFactsBlock,
  tipCardBlock,
  dayCardBlock,
  photoRowBlock,
  statRowBlock,
  budgetTableBlock,
  packingListBlock,
  pullQuoteBlock,
  inlineListingBlock,
  compareTableBlock,
  verdictCardBlock,
  whoIsItForBlock,
  destinationSectionBlock,
  distanceChipsBlock,
  deciderGridBlock,
]
